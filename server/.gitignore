# Binaries (anchored to the module root so they don't match the cmd/api/ source dir)
/bin/
/cabin
/api

# Database & uploads (local runtime data)
*.db
*.db-shm
*.db-wal
/uploads/

# Env
.env

# OS
.DS_Store
